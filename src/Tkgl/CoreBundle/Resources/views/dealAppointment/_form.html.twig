<div class="row">
    <div class="span5 offset1">

        {{ form_row(dealAppointmentForm.appointment.attendees) }}
        {{ form_row(dealAppointmentForm.appointment.subject) }}
        {{ form_row(dealAppointmentForm.appointment.location) }}
        {{ form_row(dealAppointmentForm.appointment.appointmentDescription) }}
        {{ form_row(dealAppointmentForm.appointment.startTime) }}
        {{ form_row(dealAppointmentForm.appointment.duration) }}
    </div>
    <div class="span5">
    </div>
</div> 
<div style="display:none"> {{ form_rest(dealAppointmentForm) }}</div>
<div class="control-group">
    <div class="controls">                      
        <a class="btn btn-white" href="{{path('user_manage')}}">Cancel</a>
        <button type="submit" class="btn btn-primary">Save changes</button>
    </div>
</div>
