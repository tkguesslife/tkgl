{{form_row(teamForm.name)}}
{{form_row(teamForm.teamDescription)}}
{{form_row(teamForm.province)}}
{{form_row(teamForm.region)}}
{{form_row(teamForm.teamUsers)}}
<div style="display:none"> {{ form_rest(teamForm) }}</div>
<div class="controls">                      
    <a  class="btn btn-white cancelForm">Close</a>
    <button type="submit" class="btn btn-primary">Save changes</button>
</div>
